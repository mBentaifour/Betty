#include "main.h"

/**
 * print_islower - a function that checks for lowercase character,
 *
 */

void functions_nested_loops(void)
{
	int c;
	        if (c > 96 && c < 123)
	        	return (1);
		return (0)
}
