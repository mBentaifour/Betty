#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @a: int a
 * @b: int b
 * Return: int add a , b
 */

int add(int a, int b)
{
	int res = 0;

	res = a + b;
	return (res);
}
